def mock_llm(prompt: str) -> str:
    """
    Simulador de respuesta IA para MVP / demo
    """
    return f"""
[RESPUESTA SIMULADA - MVP]

Con base en la información proporcionada, la empresa evaluada podría
operar en el terreno analizado bajo el régimen aplicable, siempre que
cumpla con los requisitos normativos específicos del sector.

La cercanía al puerto y la clasificación de la zona representan una
ventaja competitiva relevante. Se recomienda validar permisos
ambientales y municipales antes de la instalación.

(Respuesta generada a partir del prompt contextual)
"""
